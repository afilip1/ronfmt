#![enable(b, a,)]
#![enable(c)]
//     foo

// bar
(           axes:{
        "left_paddle": Emulated(
            pos: Key(W
            ),neg:
 Key(S),
        ),

"right_paddle":    Emulated(
            pos         :
             Key(Up),
                        neg:
            Key(
                        Down),
                    ),
    }
    ,

    actions: {

    },
)
